import { Header, Logo } from "./styles";

const NinjaHeader = () => {
    return (
        <Header data-testid="ninja-header">
            <Logo />
        </Header>
    );
};

export default NinjaHeader;
